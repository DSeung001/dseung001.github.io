# 후기


# 파이썬 소프트웨어의 나이테
당근에서 4년일하며 시스템이 변한 과정을 설명 
- 코드 구조 변환? 왜 바뀔까?

사내 개발자들을 위한 클라우드 리소스를 담당하는 백엔드 (사내 툴)
- 변경사항을 알려주는 비용을 줄이기 위해 
- 당근 마켓 개발자 플랫폼: 개발자가 직접 관리하는 클라우드 리소

코드 규모 섹션을 나누건 흥미롭네

22년 7월에 입사해서 redis Api 만들고 
api는 버전관리가 필요 

1. fast api 에서 의존성 주입하네 파라미터로 AWS ARN(Amazon Resource Name)은 AWS에서 생성한 특정 리소스를 고유하게 식별하기 위한 이름
=> 간단한 로직 변경점이 쉽네
- 당근 내에서 클라우드를 개발팀에서 관리하는 맞나는 의문이 잇음

2. 프로바이더를 넣어서 확장성있게 바꿨네? aws를 뺴고 싶었나
- 다른 클라우드를 넣을지는 정해지지 않았지만 넣음
- 레디스는 서브넷 그룹? 처음 들어보는데 통신라인 만들고 클러스트 만들고, 지울떄도 두개 관리 AWS 기준
- aws 연결없이 모킹을 토안 테스트 가능
- 관리 파일이 는건 어쩔 수 없지
- 처음 요청 서브넷과 클러스트를 만드니 시간이 걸림

3. 첫 요청 시간 걸리는걸 막기 위해 클라이언트를 미리 생성하자
- 내부에서 프로젝트가 인정받기 시작
- 성능 문제
  - 리전 4 x 환경 2(prod, alpha) = 8개를 관리
  - 모든 리전과 환경을 순회해야할까? 
  - 환경과 리전별 계정 건도 거슬림
  - 클라이언트를 미리 만들 수 없을까?
  - 지금은 20 x 8로 바뀌어서 미리 만드는걸로 해결

4. 서비스 레이어 추가
   - 데이터 조립등의 비즈니스 로직을 분리해냄
   - 슬랙을 통한 알림
   - 이름, 태깅 등등
   - http 없이 서비스 로직 테스트 가

5. DB 도입
- 디비를 최대한 늦게
- arn 관리, 데이터 묵을 . 있음
- db랑 aws redis의 정보를 어떤걸 할까, 원천은 aws로 둠
- db가 죽으면 서비스 죽음

6. background teaks vs celery
- fast api에서 celery로 
- 워커를 통한 분리로 api 프로세스랑 따로 때내서 서버 업데이트와 분리됨 프로세스 분리가 좋네
- 백그라운드가 돌고 있는지를 체크할 필요없음
- 대겨일 체크 가능
- 작업 실패를 알 수 있음

7. 서비스 공통 로직 추출
- 서비스 추가시 로직을 빠트릴 . 없음
- 프로젝트 자체 규칙생성및 코드로 관리
- 로직을 부모클래스에서 담당
- 대신 신규 입사자의 온보딩에 시간이 걸림


회고
불편이 생겨야 수정을 하자, 명확한 이유가 있어야 재미/성과를 챙김
코드 흐름이 점점 어려워지지만, 리팩토링 코드가 점점 늘어잠

테라폼같은 다른 시스템은 한계를 느껴서 만듦
아무래도 이쪽은 개발 영상을 봐야할듯
=> 시스템은 커스터아미징에 한계  회사별로 자체 앱을 만드는게 당연한 시대 인듯 

이런식으로 sw 생애개발주기? 를 다루네
이게 이상적인 개발기같은데, 
지금 나는 공부용이므로 시나리오를 작성하고 단기간내에 증명하려고 하니 코드 양이 복잡해지고 체크할 부분이 비대해진느게 느껴진다.

# PyArrow와 pandas 3.x - 현대적 데이터 파이프라인
추천 시스템 위주의 프로
1. 판다스 한계 
   - 일반적으로는 없는데, 판다스 만든 사람이 어느정도 한게를 적은 10년정도 전의 . 
   - 메모리애 데이터를 올리는 방법의 오류?
   - 메모리 효율을 위해 사정으로 인해 오류가 있는데, 별도의 arrow 포멧을 만듦, 컬럼 기반의 데이터 형식을 통해 효율을 높이겠다
   - 이 데이터 타입의 판다스 문제는 당장은 해결을 못함
   - arrow를 쓰려면 파일 포멧을 고쳐쟈아하는데, 이걸 쓰면 이점을 얻음
   - 즉 판다스랑 arrow 파일은 별개임
2. Py arrow
   - R, numpy 같은 데이터 판단에서 다 쓰임
   - 메모리 맵 기반 직렬화 
   - 램보다 큰 데이터를 쓸 수 있음
   - null을 따로 떔
   - 데이터 팀에서 데이터 단위는 굉장히 큰 문제구나
   - 파일 포맷을 남기는 둠, 기술 종속보다는 파일 자체로 두는걸 원하구나 (기업이나 개인이나)
3. parquet(압축률, 세상의 모든 정보는 s3에 있다 우리 회사 정보는.. 메모리 압축을 하기 위해 이걸 쓰는 구나
  - parquet는 높은 압축률 떄문에 씀, 원래 스파크에서 쓰던거,컬럼 기반 데이터 방식
  - parquet -> arrow -> pandas로 하게 되면 이상적 => s3 비용
  - 회사에서 중요한건 비용이니 우리도 비용적으로 접근해야함
4. duck db
   - db를 안쓰는 사람들이 있구나 추천은 시계열(db는 select *?)
   - 덕 db는 시계열에서 최적화 된 유사 시간을 가져오는 기능이 있구나 (이게 유일한 장점)
   - parquet를 쓰면 덕 디비의 이점을 쓸 수 있네 

판다스는 숫자 뺴고 다 오브젝트
람다는 오브젝트라고 가정한 코드가 있느데 다 터지는데, 데이터 타입이 정말 중요한게 이 분야구나 
특히 데이터는 데이터 타입의 종속되니 망가지지않도록 주의해야하구나 da 분야도 재밌네

판다스 데이터 추론?이 비 결정론적인가???
특히 릴리즈 노트에 데이터 변경건이 정말 중요한 분야 
이 분야는 워닝이 중요, 개발언어 다룰 때 이걸 좀 낮게 봤느데 데이터 타입단으로 가면 이걸 써야하네

회사 정보는 클라우드에 저장 -> 클라우드니 보안보다는 비용에 중점을 두자 (개인 의견이네)
분석은 판다스랑 덕을 합치 + parquet, 데이터 타입이 함부로 추론되는 건 주의


이게 재밌네 => ㅂ복사 비용을 줄이는게 핵심, 형식변허한을 최소화하며 비용감소하는게 사랑받는 개발자
20분안에 내용이 어렵지 않고 꽉곽 재밋게 해주네 
저장은 parquet 메모리는 arrow , 분석은 판다스, 자동화는 airflow 

폴라스?는 목적이 다름
기술 수명은 길다, 사람의 기술 수명 100년

1. 조인/groupby 하고 가져올 기다리며 커피를 먹어야할때가 빅데이터다
2. 사람의 기술 수명은 100년이다 천천히 가자
3. ai는 튜토리얼 학습시킨 다음 진행해야 버전 문제가 발생안 

# Python의 점 (.), 그 뒤에 숨겨진 7가지 이야기
우리가 파이썬에서 매일 사용하는 점(.) 연산자(Attribute Lookup)는 단순해 보이지만, CPython 내부에서는 우선순위에 따른 7단계의 복잡한 탐색 과정을 거칩니다.
파이썬 .을 최적화하는 과정, 구글 출신에 개발자도 처음에는 말이 빨라지는걸 보니 사람은 사람이구나 
누구나 다 떨리는 듯 근데 차이점은 얼마만의 자기 페이스로 되찾냐, 되찾는 발표 플로우를 만드는지가 차이점인듯

파이토치를 이길려고 했는데 같이 죽어서 나왔다 => 처음에 바로 유쾌하게 잘 녹여나네 오늘 마이크로 토크, 여기서 짬바 차이인가
파이썬의 `.`에 대한 오버헤드 => 생각보다 흥미로운 주제 선정 (tf 기준으로 파이토치에 비해 텐서가 엄청 느림)

파이썬 코드 실행이 가능한 빨리 cpp 로직에 도달해야한다.. 음 파이썬은 래핑일뿐인 느낌이네
pybind를 최적화도 해서 엄청 마이크로 벤치마크 돌리며 최적화 했네

이제 .을 실행하며 돌아가는 일
- 옵티마이징: cpython entry 앞에 로직들을 건너뛰기 
- 데이터 세팅하는것도 매직 함수 수정해서 제한을 걸 수 있긴한데 평범헌데, 밑밥이구나, 데코레이터로도 가능하 
- 슬롯, 메모리 효율적인 클래스로 함, 아마 내부에서 객체 생성 레이어 몇개를 스킯하는 걸 334 -> 64b로 줄이는 c와 가깝게 메모리 할당이 가능한데
- __dict__ 는 꽤 처리가 된 뒤에 
- 마지막도 __getattr__ = 첫 시작도 __getattribute__ 이거는 파이썬 언어 철학? 파이썬

성능극한으로 최단 c++ 바인딩, 다음은 최적화 여지가 있는 부분향, 7단계 성능 
- 그냥 앞단에서 __getattribute__ 오버라이딩
- 문자열 compare을 최적화? 
  - string interning (무거운거, 짧은 문자열의 비교가 다름, 객체 포인터로 비교하므로 초기 intern 작업이 필요) 
  - 이거 c잖아 이렇게 해결했구나 포인터로 문자열 비교하는 방식으로 초기 등록에서는 어떻게 문자열로 포인터로 저장했지?
  - Interning 메커니즘은 파이썬 기본 기능
- SIMD를 통한 가속이 되네, 하드웨어 성능인가
- 파이썬의 unused bit를 쓰느걸로 버전업하면 이 부분 체크하는걸 감안하기로 했네, cpython에 있음, 여기에 주소 값을 박는걸로, 주소를 가지고 있으니 바로 체크하면되니 해시를 안해도 되고
  - 이거는 쫄려서 안함 파이썬을 안쓰고 c++로 가겠다는 느김이네
  - 파이썬의 최적화는 사실상 c에 어느정도 가까워지냐는 느낌이구나 메모리, 성능, 효율을 위해서는 이게 맞지 

아 이글은 21년도 기준
pybind로 하는거고 1단계에서 최대한 빠른 부분에서 최적화로 빠지는거
그 이후 단계는 당연히 느릴 수 밖에 없지

처음이 근데 파이토치보다 느려서고 음.. 이런 내 영역 밖에 강의는 뭘 물어봐야하지 

# ASGI on Pyodide
브라우저에서 asgi 서버 띄우기
그리고 영어로 진행되는 데 요즘은 실시간 번역은 기본이네 파이콘 도장깨기하네 ㄷㄷ 재밋겠다
같은 주제를 나아가면 독특한 환경에서 서버를 띄울 수 있음

fast api로 버전 반환하는 간단한 api 엔드포인트
브라우저 종속이라 싱글을 쓰고 브라우저에 위탁

# python DLC

동국대생이랑 차이가 어떨지 궁금하고, 이 친구는 대본보고 안하네, 그래서 절고 흐름이 매끄럽지 않지만 난 이게 좋아
이 주제로 어디까지 발표할 수 있을까
싸피에서 본 친구네 

파이썬의 DLC?
파이썬에서 C를 쓰기 익스텐션

파이썬 인터프리터는 c로 작성되어있길래 부가 옵션으로 c로도 파이썬 모듈로 작성이 가능,
C API로 동작 => 모듈


이게 ai한테 물어봤을 때랑 결과적인 차이점은 무엇일까 
결국 주관이고 경험이지 사람의 요소가 들어가야함

오랜만에 코드를 보니 새롭네 

파이썬 import lib은 재귀로 동작하는 데 이게 내부 인터프리팅 단에서 가능한 이유는 수평적으로 먼저 부트스트 해서 가능케한데

파이썬에서 c로 메모리를 다룸

Q: C를 쓴다는건 메모리랑 연산의 최적화인데 이를 적용한 로직이나 케이스들이 있는지
A: 파이썬/c 의 시간 비교로 해본 예제가 있네 

굉장히 솔직히 불필요한거까지 설명하네


---
# 데이터 엔지니어는 왜 Pandas를 버리고 Spark를 쓰게 되는가?

도구 선택 기준
- 단일은 판다스, 인메모리에 담기고 거기에 파이썬 객체니 데이터 증폭이 됨
- 분산은 스파크,

음 내가 생각한 튜토리얼에 가까운 주제 가깝다
파이콘 연설하는 주제의 난이도를 생각할까? 아니면 이는 동국대생 세션이라서 그런가 
주재를 쭉 들어보니 앞으로의 진행이 다 눈에 보이는데
아마도 대용랭 파일 처리가 필요해졌다 -> 판다스는 단일 스레드여서 문제 발생 -> 스파크 사용를 사용 -> 상황별 비교로 정리 

수직확장은 한계가 있으니 수평확장 -> 스파크 나오겠
레이지 evaluation 실행전에 작업 계획 수립 -> DB도 이러던데 데이터 관리툴은 전부 계획 수립을 하네 
여기도 계획 수립을 비중있게 다루고 

Q: 제가 DA 직무에 대한 이해도가 낮지만 하나 궁금한점이 판다스의 문제점으로 짚어준게 파이썬으로 데이터 프레임으로 래핑하면서 늘어난 메모리 문제 때문에
다른 걸 염두로 두고 서칭해보고 거기서 스파크를 찾은건데, 여기서 다른 랭귀지나 다른 도구들도 고려해보셨을 텐데
그 중에서 인상 깊었던 언어, 도구 등이 있을까요?
A: 선택지가 나오는데, 요구사항에 맞춰 달라진다고 ppt 안에서 나오네, 실무 경험을 물어보기엔 2달차라 좀 미안하고 qna 세션이 없음

음 신입이 도구 선택했고 선택 이유 발표하는거랑 큰 차이가 없는 느낌

# 당근에서 서포팅 만든거
파이썬이 가진 특징
- 개발 편의 운영에서 어렵다는 점
- 대기업은 영어를 다하나? => 비중이 궁금
- 문제는 파이썬 언어팀에서 뭘할지를 고민 (코어 단 얘기네)
  - 인증 라인, 디버깅 방법

언어 소프트 팀
어플 -> 플랫폼 -> 오픈 소스
----------------------
이들을 아우르는 랭귀지 서포트 팀, 대규모에서 적용되는 이야기네 

구글/메타/마소에서나 있는거고 회사마다 다름 => 단일 모델 x
큰 회사마다 자체 파이썬을 빌드해서 관리하는데
작은 링크드인도 하고, 작은 20명도 안되는 회사도 하는데 이유가 뭘까?
=> 해결하고 싶은 문제에 맞춰서 빌드한다, 과연 어떤 문제일까 궁금하네 


여기도 ai에 대한 문제 답변은
비용/근거네 BE랑 다를바 없구나

아예 qa가 없구나
---

당근 이야기 

회사에 파이썬 서포트 팀은 없는데, 자원봉사 느낌
Q: 파이썬이 대부분 코드를 지니고 있는데, Go 컨퍼런스에도 많이 보였고 지원 공고에서 Goㄹ를 쓴다 봤는데
영역이 어떻게 갈리지??
=> 팀마다 다르다고 정정하네 언어별로 서포트 팀을 만들자는 이야기

그래서 슬랙을 통해 생겨난 작은 문화
로깅/패키지/트러블슈팅/교육(신규 기능)

light llm? 이거 맞나 
월간 미팅에서 기술적 미팅을 하네 

언어를 잘 쓰고 커뮤니티에 연결
supply chain 공격
OWASP 서울? 이건 뭐지 

15분만에 긑나고
파이썬 언어 팀을 만들고 이런게 좋다는 말이네 

폴리글라스?

Q :
소규모팀에서 20인이하에서 언어팀을 하는 게 흥미로웠는데 어떤 문제를 해결하기 위해 했는지가 궁금하네 
처음 들었을 때는 언어팀이라는게 대규모 개발 인력이 있을 경우에만 효과를 발휘하려는거 같은데 
소규모팀에서 거기에 리소스를 넣는다는 건 어던 목적을 해결하기 위함이었는지? 또는 나동희님이 생각하는 소규모팀에서 언어팀이 필요한 경우가 어떤걸로 생가하시는 지 궁금함
A: 퍼포먼스를 위해서로 추측, 그만큼의 이유는 도메인에 종속되는거니까 아무래도 당장 말하기는 힘들네

1. 언어 팀의 확장, 커뮤니티와의 연동
=> 해외 사례가 많고 여기에 사람을 어떻게 모았는지?, 사내에서 모임?

2. 라인/야후에서 언어 소프트팀을 가져온 이유
=> 아시아팀에서 돌 수 있다는 걸 증명하고 싶었음, 골드 이미지라고 표현하네, 골든 데이터 셋이랑 연동되나보나

여기는 질문 위주로 돌아가구나 
좀 더 질문 시간에 비중을 주는 구나 

3. 비즈니스 로직에서 사람과의 충돌, 감정 부분을 언어 팀의 솔루션을 거부하거나 부정적으로 받아 들이는걸로 커뮤니케이션을 어떻게 했는지?
=> 솔루션별로 전문가가 별도로 있음, 당근은 이쪽으로 굉장히 열린 컬쳐구나 다툼 보다는 적용이 우선되는 방향이여서 충돌이 발생하진 않는 분위기 

4. 스터디 방향은 어떻게 했는지, 파이썬 딥 다이브
=> 거기서 코어 중심으로 딥다이브, 논문이나 프리쓰레딩 같은걸 바이런스 레퍼런스 논문 등을 코드레벨로 하나하나 공부하고 스터디하는 방향으로 가는데
이런 쪽으로 해야 코어 레벨 공부군 이렇게 해야

== 결론
내게 느껴지는 콘 코어 레벨을 퍼포먼스에 관심을 가지고 하는 부가적인 업무로 느껴지고 이를 재밌어한다면 굉장히 좋은 문화인듯 하다.
아직 당근에서 이 언어팀은 초기라서, 내년을 기다리면서 1년간에 자료가 쌓이는게 좋을듯
나중에 스킬이나 mcp 서버로 묶을 수 있겠네

---