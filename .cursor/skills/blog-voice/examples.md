# 말투 예시

실제 글에서 고른 짧은 인용이다. 문장 리듬만 맞추고, 주제나 수치를 베끼지 않는다.

## 상황으로 열기

출처: `content/posts/2026/09/04/class-s-gevent-greenlet/index.md`

```
갑자기 운영 중이던 서버에서 여러 개의 메일이 발송되어서 놀랐던 적이 있습니다.
잘 운영되다가 갑자기 새벽 시간에 이렇게 서버에서 에러가 발생한다는 게 신기했기에, 최근에 SEO를 손봐서 봇이 접근한 걸까라는 의문이 들기도 했죠.

아래는 내용의 일부입니다. `too many clients already`로 PG에서 허용된 connection보다 더 많은 개수가 부여된 걸 알 수 있는데 여기서 의문이 들었죠, 내 서비스는 지금 일일 사용자가 10명도 안 되는데?
```

장애, 비용, 의문처럼 실제 계기로 연다. 개념 정의로 시작하지 않는다.

## 비용과 추론

출처: `content/posts/2026/08/27/class-s-stt-cost-optimization/index.md`

```
이전까지는 하루 강의치를 전부 올리면 대략 $1.2~$1.5 선으로 나왔습니다.
이 비용은 서버 비용까지 합친 게 아니라 오직 AI 비용이며, 하루에 4~5시간짜리 영상을 올리면 여기서 텍스트를 추출하고 임베딩하는 비용 전체를 나타내죠.

처음에는 MVP였기에 가장 사용이 많은 OpenAI의 `Whisper`가 자료가 많으니 이를 택했습니다.
이제는 해당 기능이 어느 정도 안정화되었으니, 비용 최적화를 진행해봅시다.

5분짜리 강의 샘플로 실험해 보니, 무음을 잘라 말 구간만 이어 붙이면 길이가 약 300초에서 272초로, 대략 10% 줄었습니다.
처음에는 오디오를 편집함으로써 안정성이 떨어질까 걱정하였지만 샘플링 테스트 결과는 오히려 더 안정적으로 느껴졌습니다.
```

수치에는 단위와 전후를 같이 쓴다. 처음에는 X, 그래서 Y, 결과는 Z 순서로 적는다.

## 용어 정의

출처: `content/posts/2026/08/12/postgres-fts-vector/index.md`

```
Class S를 만들었던 지난 [하이브리드 검색](../../07/class-s-hybrid-search/) 글에서는 텍스트를 토큰으로 나눠 검색하는 FTS 방식과, 임베딩 모델을 거쳐 수치로 표현해 비교하는 벡터 검색 방식을 사용했습니다.
전에는 이 두 방식을 로직 흐름으로 짧게 지나갔지만, 이번에는 더 면밀히 봐봅시다.

FTS는 Full Text Search입니다. `LIKE '%키워드%'`보다 키워드와 토큰 검색에 적합하고, 보통 `tsvector`, `tsquery`, `GIN index`를 조합해서 사용합니다.
```

이전 글을 내부 링크로 잇고, 용어는 처음 나올 때 한 줄로 정의한 뒤 본론으로 들어간다.

## 피해야 할 문체

같은 주제를 일반 LLM 문체로 쓰면 아래처럼 된다. 이렇게 쓰지 않는다.

```
본 글에서는 PostgreSQL 커넥션 고갈 이슈를 정리합니다. 핵심은 세 가지입니다.

1. gevent 워커의 동시 연결 수가 과도하게 늘어날 수 있습니다.
2. Django의 CONN_MAX_AGE 설정이 커넥션 재사용에 영향을 줍니다.
3. 사이트맵 팬아웃이 순간 트래픽을 유발합니다.

정리하면, 워커 상한을 낮추고 커넥션 수명을 조정하는 것이 권장됩니다.
```

정의 나열, "핵심은 N가지", "정리하면", "권장됩니다"로 닫는 요약체는 이 블로그 말투가 아니다.
